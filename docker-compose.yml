version: "3.9"
name : internship
services:
  postgres:
    image: postgres:17

    container_name: postgres_db

    restart: always

    environment:
      POSTGRES_USER: divyanshi
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: mydatabase

    ports:
      - "5432:5432"
