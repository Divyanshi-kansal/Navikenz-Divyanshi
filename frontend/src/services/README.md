# Services

The `services` folder contains the function responsible for communicating with the FastAPI backend and separates API logic from the user interface.

---

## Responsibilities

- Send HTTP requests to the backend
- Receives API responses
- Handle authentication tokens 
- Keep the frontend and backend communication seperate.

---

## Files

`services.js/`
Contains all API service functions used throughout the frontend application.

---